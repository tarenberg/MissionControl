# 2026-03-07

## 🚀 Session Summary
- **User:** tberg. (LooselyTwisted)
- **Focus:** Fixing PHP Art Portfolio & Building "Second Brain" (Proto-Qwestor).

## 🛠️ Infrastructure & Environment
- **Web Server Split:**
  - **IIS (Port 80):** `C:\inetpub\wwwroot` (Stopped/Disabled).
  - **XAMPP Apache (Port 8080):** `C:\xampp\htdocs` (ACTIVE).
  - **URL:** `http://localhost:8080/`
- **PHP Config:**
  - `php.ini` has `curl` enabled.
  - Localhost requires `CURLOPT_SSL_VERIFYPEER = false` to talk to Google APIs.

## 🎨 Art Portfolio Project (`C:\xampp\htdocs`)
- **Security Fix:** Moved API Key from `index.php` (frontend) to `config.php` (server-side).
- **Backend Proxy (`api.php`):** Created to handle chat requests securely.
  - **Endpoint:** `POST /api.php`
  - **Model:** Must use `gemini-2.5-flash` (older `1.5` and `pro` models are deprecated/404).
- **Frontend (`index.php`):**
  - Updated chat logic to use `fetch('/api.php')` (absolute path required).
  - Removed Google GenAI JS SDK (now handled by PHP).
- **Debug Tools:** Left `test_chat.php`, `list_models.php`, and `simple.php` in root for future diagnosis.

## 🧠 Second Brain (Proto-Qwestor)
- **Location:** `C:\Users\tberg\.openclaw\workspace\BRAIN`
- **Architecture:** Separates "Brain" (State) from "Hands" (OpenClaw).
- **Components:**
  - `goals.json`: Active goals and weights.
  - `memories.json`: Long-term knowledge store.
  - `governor.py`: CLI tool to manage state (`--status`, `--add-goal`, `--add-memory`).
- **Status:** Fully operational. Documented website architecture into the Brain's memory.
