

## 🖥️ Ops Control Screen Proposal & Implementation Plan
- **User Request:** Add a new "Ops Control" screen to Mission Control to show details of all scheduled and background activities. Specs provided included data sources (cron jobs, heartbeats, internal/external schedulers, processes, monitors, manual deadlines, alerts) and screen organization (top summary bar, two-column grid with timeline/queue and tabbed details/active processes, bottom log stream).
- **Documentation:** Recorded the full Ops Control specs in `docs/mission-control-team-screen-concept.md` under a new "Ops Control" subheader.
- **Proposed Improvements (by Muffin):**
    - More actionable "Recent Failures" status in the summary bar.
    - Explicitly define interactive "Run/Disable/Edit" button behaviors to use `openclaw cron` commands.
    - Refined "Muted Alerts" logic/UI for clarity and interaction.
    - Graceful handling for "External Schedulers" (Windows `schtasks`) on non-Windows environments.
    - Refined content examples for "Resource Monitors" for better clarity.
- **Detailed Outline for Jason (Claude Code) for Ops Control Screen Build:**
    - **Data Layer (`lib/opsControlData.ts`):** Defined detailed TypeScript interfaces and async server action functions (`getCronJobs`, `getHeartbeatInfo`, `getInternalSchedulers`, `getActiveProcesses`, `getExternalSchedulers`, `getResourceMonitors`, `buildTimeline`, `buildActivityLog`, `loadOpsControlData`) to fetch and aggregate all required data.
    - **UI (`app/ops-control/page.tsx` & `components/OpsControl/*.tsx`):** Detailed rendering instructions for the main server component, `SummaryBar`, `MainGrid` (with `TimelineList` and `TabbedPanel`), `JobDetailsPanel`, `ActiveProcessesPanel`, and `LogStream`.
    - **Navigation & Styling:** Instructions to update `app/layout.tsx` for navigation and ensure consistent Tailwind styling.
    - **Action Functions:** Defined placeholder client-side action functions (`runJob`, `toggleJobEnabled`, `killProcess`) to be implemented later.

## 🎨 Art Tracker Application: "Add New Show" Button
- **User Request:** Add a "Add New Show" button to the "Shows & Calls" section within the ArtTracker application (specifically `C:\xampp\htdocs\tools\ArtTrackerDashboard\src\Dashboard.tsx`).
- **Initial Research:** Attempted to locate "Chosen Calls" in the ArtTrackerDashboard, but `findstr` results were unrelated to UI components.
- **Next Steps:** This task will be handled by Jason after the Ops Control screen is built. The plan will involve modifying `src/Dashboard.tsx` to include state for adding a new show, a new button, a modal for input, and a `handleSaveShow` function to send data to the `api/shows.php` endpoint.

## 🤖 Agent Operational Status & Setup
- **Jason (Claude Code) Testing:**
    - Confirmed `codex` CLI was actually a static site generator, not the intended coding agent.
    - Switched to using `claude` CLI for Jason.
    - Successfully performed a smoke test with `claude --permission-mode bypassPermissions --print "Create or overwrite hello.txt with the line 'Jason (Claude Code) is online.' Then run dir."` in a temporary git repo.
    - Verified `hello.txt` content.
    - Cleaned up the `jason-smoketest` directory.
- **Future Code Generation:** Confirmed with user that all future code generation and refactoring tasks should be handled by Jason (Claude Code).

