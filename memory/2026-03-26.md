# 2026-03-26

_No highlights yet. TODO: record highlights as the day progresses._
## 2026-03-26 Update
- **Picture Hanger Pro 3**: Contains the completed Molding Processing system (clarified, not part of Composition Pro).

## ArtTracker & Mission Control Fixes
- **ArtTracker Data Issue**: Artworks were returning empty JSON because `json_encode()` failed on special characters in descriptions (like em-dashes). Fixed by explicitly setting `charset=utf8mb4` in `new PDO()` across all API endpoints (`artworks.php`, `costs.php`, `deadlines.php`, `shows.php`).
- **ArtTracker Caching**: Browsers aggressively cached the broken CORS preflight responses. Added `Cache-Control` headers to PHP and `?t=Date.now()` to the frontend fetches.
- **Mission Control Home (`/`)**: Replaced hardcoded model lists with a dynamic `/api/models-status` route that directly queries `openclaw models status --json` and `ollama list`.
- **Mission Control Team (`/team`)**: Corrected the visual pipeline to correctly map Muffin -> Jason -> Archivist -> Pathfinder -> Sentinel, and explicitly added Housekeeper (`ollama/llama3`).

## Subagents & API Limits
- **OpenAI Quotas**: Hit a hard 429 rate limit on `gpt-5.1-codex` and embeddings, preventing semantic `memory_search` and cloud subagent spawning.
- **Local Fallback**: Configured OpenClaw to allow `ollama/qwen2.5-coder:7b` as a subagent model to successfully bypass cloud restrictions and execute tasks entirely locally on Jason.
- **Snake Game**: Built a working HTML/CSS/JS snake game in `~/workspace/snake-game` to test local coding execution.

## Miscellaneous
- **Cron Jobs**: Scheduled a one-shot reminder cron for "Doctor Wolfson" at 12:00 EDT.
