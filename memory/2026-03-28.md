
**2026-03-28 - ArtTracker Debugging - Persistent Artworks Not Visible**
- User reports artworks still not visible in ArtTracker dashboard.
- Repeated issue with `leftColumn` computed `height: 0px`, preventing display of artworks list.
- Attempts made:
    - Verified API data fetching and image accessibility (both fine).
    - Removed all collapsible UI logic from `Dashboard.tsx` for artworks and costs sections.
    - Applied CSS changes to `Dashboard.module.css`:
        - Set `leftColumn` to `flex-grow: 1; min-height: 0;`
        - Set `dashboardMain` `grid-template-rows` to `1fr auto` (from `minmax(0, 1fr) auto`).
        - Removed `overflow: hidden;` from `dashboardMain`.
        - Added `flex: 1;` to `leftColumn`.
- Issue persists despite these changes.
- Next step: Detailed computed style inspection of `.dashboardMain` and `.leftColumn` from user's browser dev tools after a fresh restart and cache clear.
