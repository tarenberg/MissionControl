# Daily Memory – 2026-04-22

## General
- TODO: record highlights from the day.

### 2026-04-22: Agent Spawning Troubleshooting

**Issue:** User reported inability to spin up agents.

**Initial Diagnosis & Attempt:**
1.  Reviewed `openclaw.json`.
2.  Attempted to add a "test-agent" to `agents.list` and spawn it.
3.  `sessions_spawn` for "test-agent" failed with "Failed to spawn agent command: test-agent".

**Deeper Investigation (using `acp-router` skill):**
1.  Read `acp-router` skill documentation.
2.  Identified `ACPX_PLUGIN_ROOT` as `C:\Users\tberg\AppData\Roaming\npm\node_modules\openclaw\dist\extensions\acpx`.
3.  Read `ACPX_PLUGIN_ROOT/package.json` to determine pinned `acpx` version: `0.5.3`.
4.  Attempted to verify `acpx --version` in the plugin's `node_modules/.bin` directory, which failed (command not found). This indicated `acpx` was not properly installed locally.

**Solution Attempt:**
1.  Executed `npm install --omit=dev --no-save acpx@0.5.3` within the `ACPX_PLUGIN_ROOT` to install the pinned version of `acpx`. Installation was successful.

**Next Steps:**
1.  Restart the OpenClaw gateway.
2.  Retry spawning the "openclaw" agent with a simple `echo` task to confirm the fix.
