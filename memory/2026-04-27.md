# Daily Memory – 2026-04-27

## General
- TODO: record highlights from the day.

## Gateway Instability and Model Failures

- **Initial Report**: Tom reported "a lot of problems with the gateway".
- **Investigation**: I checked `openclaw gateway status` (which appeared normal) and then the logs (`\tmp\openclaw\openclaw-2026-04-27.log`).
- **Finding 1**: The logs showed a recurring warning: `failed to start server "mempalace"`. This indicates a problem with the bundled MemPalace service.
- **Action 1**: I attempted to resolve the issue by restarting the gateway with `openclaw gateway restart`.
- **Finding 2 (Post-Restart)**: The restart did not solve the MemPalace issue. A deeper log review revealed a cascade of critical failures:
    - **API Quotas Exceeded**: Both Anthropic (Claude) and OpenAI (GPT) models are inaccessible due to hitting billing/rate limits.
    - **Local Model Misconfigured**: The local Ollama model (`gemma2`) is failing because its context window is set to 8192, which is below the required minimum of 16000.
- **Status**: The gateway is technically running, but is functionally down because it cannot access any of its configured language models and a core service (MemPalace) is broken.
- **Next Steps**: Advised Tom to check the billing dashboards for his Anthropic and OpenAI accounts as the most urgent priority. After that, we need to reconfigure the local `gemma2` model and troubleshoot the MemPalace service.
