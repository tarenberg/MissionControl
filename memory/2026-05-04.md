# 2026-05-04
*   **Breadcrumb UI Polish**: Removed 3D button effects from document breadcrumbs for better readability.
*   **Dark Mode Implementation**: Added a global dark mode theme to Mission Control.
    *   Implemented CSS variables for theme-aware colors (background, card, sidebar, border).
    *   Added a `ThemeToggle` component in the sidebar with persistence to `localStorage`.
    *   Updated primary dashboard components (System Monitor, Studio Bridge, Art Deadlines) and the Docs Browser to support dark mode.
    *   Enhanced markdown rendering with `dark:prose-invert` for high-contrast documentation.
*   **Nightly Sprint (Muffin 🧁)**:
    *   **Ops Log Monitor**: Implemented a real-time, terminal-style log viewer on the dashboard. Created a backend API to stream the last 50 lines of Gateway and App logs via PowerShell.
    *   **Art Deadlines**: Refreshed `art-deadlines.json` with 24 new potential deadlines.
    *   **UI Polish**: Refactored `AgentOrgChart.tsx`, `AgentCard.tsx`, and the new log monitor using the "Loosely Twisted" neomorphic design system (Soft UI).
    *   **Identity**: Integrated a new animated logo component (`Logo.tsx`) with hover-reset logic.
    *   **Optimization**: Locked specialized agents to economy-tier models (Gemini Flash, Ollama) and switched global fallback to Gemini Flash to control Anthropic spend.
    *   **Staging**: All work committed to branch `muffin/ops-log-monitor`.

