# Daily Memory – 2026-05-10

## General
- TODO: record highlights from the day.
## 2026-05-10 | Muffin | Session Summary

### 🛠️ Infrastructure & Stability
- **Gateway Watchdog Recovery**: Fixed a critical PowerShell error in `gateway_watchdog.ps1` caused by attempting to overwrite the reserved `$pid` variable (renamed to `$zombiePid`).
- **Health Check Hardening**: Upgraded the watchdog from simple port-listening detection (`netstat`) to an active health check using `Invoke-WebRequest`. It now forces a restart if the gateway is unresponsive even if the port is open.
- **Log Noise Reduction**: Updated `monitor_logs.ps1` to ignore routine security warnings and payload character budget notices, preventing spam in `tasks/todo.md`.
- **Agent Performance**: Standardized Housekeeper agent on `google/gemini-2.5-flash` to eliminate the 60-second Ollama failover delays observed on startup.

### 🎨 Art Tracker: Nightly Sprint
- **Show Discovery Inbox**: Implemented a "Discovery" widget in the dashboard sidebar that pulls from scraped web data.
- **Persistence**: Created `app/api/art-deadlines/route.ts` to handle opportunity display and a local dismissal list (`data/dismissed-deadlines.json`).
- **Scraper Expansion**: Added `artshow.com` to `fetch-art-deadlines.js`, bypassing Cloudflare protection and increasing nightly yield to 74 total deadlines.
- **AI Cost Syncing**: Fully integrated `sync-ai-costs.js`. Muffin now automatically syncs Gmail receipts (Anthropic, OpenAI, Midjourney, OpenRouter) to the PHP costs backend with duplicate detection.

### ✅ Database Maintenance
- **Path Normalization**: Standardized image URL handling in `deadlines.php` to ensure thumbnails load correctly across both local and Tailscale environments.
## 2026-05-10 | Mission Control & Art Tracker UI/Backend Updates

- **UI Standardization**: Updated all refresh buttons in `ArtTrackerDashboard.tsx` to match the neomorphic style of the Studio Bridge (`StudioMonitor.tsx`). Replaced legacy text buttons with icon-based `RefreshCw` triggers featuring `animate-spin` loading states and `neo-button` styling.
- **Layout Optimization**: Reordered the right column of the Art Tracker dashboard to prioritize `Dynamic Deadlines` at the top, followed by `Show Discovery` and `Shows & Calls`.
- **Scraper Hardening (Artists Network)**: successfully integrated `https://www.artistsnetwork.com/art-competitions/` into the automated scraper (`fetch-art-deadlines.js`). Resolved a persistent 403 Forbidden/Cloudflare block by implementing a specialized proxy bypass via `r.jina.ai`.
- **Specialist Deployment**: Utilized **Scout** (optimized on Gemini Flash) to investigate scraper blocks and provide the logic for the successful Artists Network integration.
- **Data Integrity**: Verified the persistence of `data/art-deadlines.json` and ensured new scraped entries (e.g., *Artists Magazine Annual*, *Strokes of Genius*) merge correctly into the Discovery Inbox.
