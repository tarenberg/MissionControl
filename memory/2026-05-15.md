# Daily Memory – 2026-05-15

## General
- TODO: record highlights from the day.
# Daily Memory – 2026-05-15

## General
- **Nightly Sprint (May 14/15)**: Successfully resolved key bottlenecks in Mission Control.
- **Media Retention**: Created `scripts/cleanup-celebrations.js` to manage storage by deleting social assets >30 days old.
- **Discovery Integration**: Built the "Discovery Digest" widget in `ArtTrackerDashboard.tsx` and a supporting API endpoint to surface Pathfinder Trend Radar data (e.g., "The New Noir" trend) directly in the UI.
- **Pathfinder Targets**: Verified priority targets including the Hamden Art League and Providence Art Club National Open are visible and actionable within the new Discovery widget.
- **System Stability**: Verified the Mission Control server (PID 51800) and prospectus worker (`plaid-rook`) remain stable.

- **Unicode Encoding Standardized**: Migrated `ArtTrackerDashboard.tsx` from raw emojis to explicit Unicode escape sequences (e.g., `\uD83D\uDCCD` for location pin) to prevent character mangling by system tools like PowerShell.
- **Gallery Interaction Patch**: Modified artwork click behavior to open the Edit modal by default. Linking logic is now context-aware: only toggles submission if a deadline is actively selected in the sidebar.
- **Submission Assistant v2**: 
    - Integrated a "Go to Entry Form" button in the `Confirm Entry` modal to streamline side-by-side submission with external portals (CaFE/EntryThingy).
    - Verified the "Double-Write" strategy where "Track Opportunity" updates both `upcoming_shows` and `deadlines` databases simultaneously.
- **Scraper Hardening**: Successfully deployed `r.jina.ai` bypass for the *Artists Network* and *ArtShow* scrapers, bypassing 403 Forbidden errors and extracting 71 new opportunities.
- **Pathfinder Research**:
    - Identified **Art of the Northeast (Silvermine, CT)** as a high-priority local target.
    - Filed an investment brief on **Atoco** (Metal-Organic Frameworks for carbon/water harvesting) in `docs/research/atoco-investment-brief.md`.
- **Infrastructure Maintenance**: Restarted `worker-prospectus.js` to clear the queue of pending AI prospectus analyses.

## 2026-05-15 (PM Update)
- **Studio Scan Tool (Nightly Sprint)**:
    - Implemented a "Scan-Flat" engine (`scripts/scan_engine.py`) using OpenCV for quad-perspective transformations.
    - Added a PHP backend endpoint (`api/scan.php`) to handle studio photo uploads, process them via the Python engine, and save results to the Hi-Res archive.
    - Features: Interactive corner pin mapping, aspect ratio preservation (anti-stretch), 5K upscaling, and 5MB target file size optimization via JPEG quality loops.
    - Created design spec at `docs/research/art-scan-tool-spec.md` and task list at `tasks/scan-tool-todo.md`.
- **Art Tracker UI/UX Patches**:
    - Resolved emoji encoding issues in `ArtTrackerDashboard.tsx` by using explicit Unicode escape sequences (`\uXXXX`).
    - Fixed "Track Opportunity" sync logic to ensure double-writing to both `upcoming_shows` and the primary `deadlines` calendar table.
    - Integrated the Discovery Digest/Pathfinder Trend Radar directly into the dashboard card view.
- **Artwork Archive Verification**:
    - Verified processing and cataloging of "Portrait of a Woman" and "Uptown" (Master, Standard, and Framed versions) in `docs/artwork-archive/inventory.md`.
    - Confirmed high-resolution master scans are correctly filed in the `.openclaw/workspace/docs/` directory for Obsidian visibility.
