# Daily Memory – 2026-05-18

## General
- TODO: record highlights from the day.
# 2026-05-18

## Nightly Sprint & Morning Kickoff (Muffin 🧁)
- **Voice-to-Studio Integration**: Expanded the Gemini 2.0 Flash Realtime pipeline. Implemented `CHECK_STUDIO` and `STUDIO_COMMAND` action handlers in the Chat Action Dispatcher (`ChatPopup.tsx`). Muffin can now theoretically control Nest hardware via voice once authenticated.
- **Studio Command API**: Implemented a `POST` handler at `/api/studio/environment` to support device commands, specifically targeting thermostat setpoints (includes Fahrenheit to Celsius conversion for the Nest SDM API).
- **Nest Auth Recovery**: Added a "Repair Connection" button and UI helper to the `StudioMonitor` component. This provides a direct path for the user to resolve `invalid_grant` errors by refreshing the OAuth token.
- **Scraper Hardening**: Verified the reliability of Jina-based bypasses (`r.jina.ai`) for `Artists Network` and `ArtShow` scrapers, ensuring continued data flow despite Cloudflare protection.
- **System Hygiene**: Cleaned up `tasks/todo.md` by removing ~800KB of automated error monitor noise that was contributing to context bloat.
- **Mission Control v2.5.1**: Performed a deep reset of the dev server to resolve a persistent "Key not set" error in the Gemini Live interface. Renamed the hook to `useGeminiLiveV4` and updated the SDK initialization to use the object-based constructor required by `@google/genai` v2.x.
- **Port Hygiene**: Cleared multiple zombie `node.exe` processes hogging port 3000 to ensure the mobile browser (via Tailscale) sees the latest code.
- **Cache Busting**: Deleted the `.next` directory and updated the `DASHBOARD_RENDER_TRIGGER` to `2026-05-18 19:45` to force a hard refresh on mobile clients.
